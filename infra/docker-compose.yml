services:
  db:
    image: postgres:16
    container_name: capresol-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: mayerattIE
      POSTGRES_PASSWORD: real_estateSolved
      POSTGRES_DB: capresol
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data